Telegram bot

usage:
  bot.py - library + when runned as main: simple demo that reverse incoming strings
  demo.py - use library to show how to register commands
  tg_sendtext.py - send text
  tg_sendimage.py - send image
